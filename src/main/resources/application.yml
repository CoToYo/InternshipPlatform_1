# 服务端口号
server:
  port: 8001
  tomcat:
    uri-encoding: UTF-8

spring:
  application:
    name: InternshipPlatform_1
  http:
    encoding:
      force: true
      charset: UTF-8
      enabled: true
  # 配置nacos
#  cloud:
#    nacos:
#      discovery:
#        server-addr: 127.0.0.1:8848

  # 配置时间格式
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

  # 环境设置：test、dev、prod
  profiles:
    active: dev

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/guli?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 12345666

#mybatis-plus:
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl  # 控制台显示sql执行详细信息

# 设置控制台日志等级
#logging:
#  level:
#    root: WARN


# 配置熔断器hytrix
# 开启feign熔断机制
feign:
  hystrix:
    enabled: true

# 设置hystrix超时时间，默认1000ms
#hystrix:
#  command:
#    default:
#      execution:
#        isolation:
#          thread:
#            timeoutInMilliseconds: 1
